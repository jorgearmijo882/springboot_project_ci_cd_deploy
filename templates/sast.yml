jobs:
  - job: SAST
    pool:
      name: Default
    steps:
      - script: |
          echo "Running SpotBugs analysis..."
          mvn com.github.spotbugs:spotbugs-maven-plugin:4.7.0:check
        workingDirectory: $(Build.SourcesDirectory)
        displayName: 'SpotBugs Static Analysis'
